#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
 * _isupper - check the code
 *
 * @c: the num to check
 * Return: Always 0.
 */
int _isupper(int c)
{
if (isupper(c))
{
return (1);
}
else
{
return (0);
}
}
