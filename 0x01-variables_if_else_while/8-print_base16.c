#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char start = '0';
char end = 'F';
char ch;
for (ch = start; ch <= end; ch++)
{
putchar(ch);
}
putchar('\n');
return (0);
}
