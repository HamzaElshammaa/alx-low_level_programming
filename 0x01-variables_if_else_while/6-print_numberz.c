#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int start = 0;
int ch;
for (ch = start; ch <= 9; ch++)
{
putchar(ch);
}
putchar('\n');
return (0);
}
