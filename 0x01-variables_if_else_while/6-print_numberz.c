#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char start = '0';
char ch;
for (ch = start; ch <= '9'; ch++)
{
putchar(ch);
}
putchar('\n');
return (0);
}
