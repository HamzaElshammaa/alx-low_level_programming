#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * _strcmp - check code
 * @s1: check code
 * @s2: check code
 * Return: int
 */
int _strcmp(char *s1, char *s2)
{
int result = strcmp(s1, s2);
return (result);
}
