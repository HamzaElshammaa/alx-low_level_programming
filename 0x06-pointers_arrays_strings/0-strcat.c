#include <stdio.h>
#include <string.h>
#include "main.h"
/**
 * _strcat - Check code
 * @dest : var one
 * @src : var two
 * Return: always 0
 */
char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
