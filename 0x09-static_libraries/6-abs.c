#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>
#include "main.h"

/**
 * _abs - check the code
 * @n: num to abs
 * Return: returns abs value
 */
int _abs(int n)
{
int res = abs(n);
return (res);
}
